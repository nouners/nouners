{
  "private": true,
  "name": "nouners",
  "engines": {
    "node": ">=22.11.0"
  },
  "scripts": {
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "pnpm -r run --if-present format"
  },
  "packageManager": "pnpm@10.21.0",
  "devDependencies": {
    "prettier": "^3.7.3",
    "turbo": "^2.6.1"
  },
  "version": "1.0.0-alpha.41"
}
