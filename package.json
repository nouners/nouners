{
  "private": true,
  "name": "nouners",
  "engines": {
    "node": "^24",
    "pnpm": "^10"
  },
  "scripts": {
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "pnpm -r run --if-present format"
  },
  "packageManager": "pnpm@10.27.0",
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.5.3"
  },
  "version": "1.0.0-alpha.51"
}
