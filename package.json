{
  "private": true,
  "name": "nouners",
  "engines": {
    "node": ">=22.11.0"
  },
  "scripts": {
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "pnpm -r run --if-present format"
  },
  "packageManager": "pnpm@10.18.3",
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.6.0"
  },
  "version": "1.0.0-alpha.33"
}
