{
  "private": true,
  "name": "nouners",
  "engines": {
    "node": "^24",
    "pnpm": "^10"
  },
  "scripts": {
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "pnpm -r run --if-present format"
  },
  "packageManager": "pnpm@10.25.0",
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.7.1"
  },
  "version": "1.0.0-alpha.45"
}
